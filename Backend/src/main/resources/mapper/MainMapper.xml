<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.모집.mapper.MainMapper">

    <select id="selectMainRooms"
            resultType="com.example.demo.모집.vo.MainVO">

        SELECT
            r.room_id AS roomId,
            r.title,
            r.start_date AS startDate,

            c2.name AS midCategoryName,
            c3.name AS subCategoryName

        FROM room r
                 LEFT JOIN category c3 ON r.categoryId = c3.id
                 LEFT JOIN category c2 ON c3.parent_id = c2.id

        WHERE r.status = 'OPEN'
        ORDER BY RAND()
            LIMIT 4

    </select>


</mapper>
