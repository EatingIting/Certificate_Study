<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.main.MainMapper">

    <!-- 메인페이지 모집중 스터디 최신 4개 -->
    <select id="selectMainRooms"
            resultType="com.example.demo.main.MainVO">

        SELECT
            r.room_id AS roomId,
            r.title,
            r.start_date AS startDate,

            c2.name AS midCategoryName,
            c3.name AS subCategoryName

        FROM room r
                 LEFT JOIN category c3 ON r.categoryId = c3.id
                 LEFT JOIN category c2 ON c3.parent_id = c2.id

        WHERE r.status = 'OPEN'
        ORDER BY r.created_at DESC
            LIMIT 4

    </select>

</mapper>
